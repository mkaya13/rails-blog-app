<%= form_with model: @comment, url: create_user_post_comment_url, method: :post, local: true, scope: :comment do |f| %>

    <div>
      <%= f.label :comment %><br>
      <%= f.text_area :text %>
    </div>
    
    <%= f.submit "Add Comment" %>
    <% end %>